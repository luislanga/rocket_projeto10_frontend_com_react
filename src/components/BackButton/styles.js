import styled from "styled-components"

export const Container = styled.div`
    >img {
        margin-right: 8px;
    }

    color: ${({theme}) => theme.COLORS.PINK};
    font-size: 16px;

    cursor: pointer;
`